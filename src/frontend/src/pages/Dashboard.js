export default function Dashboard() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-1">Dashboard</h1>
      <p className="text-[#888] text-sm mb-8">Vista global del sistema</p>
      {/* TODO: métricas, gráficas con recharts, nube de palabras con D3 */}
    </div>
  );
}